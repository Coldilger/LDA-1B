# Path was hardcoded to the authors' machine (/mnt/home/liukai/code/LDA/...),
# which does not exist here. Relative to the repo root, which every launch cds into.
compute_environment: LOCAL_MACHINE
debug: false
deepspeed_config:
  deepspeed_config_file: lda/config/deepseeds/ds_config.yaml
  deepspeed_multinode_launcher: standard
  zero3_init_flag: false
distributed_type: DEEPSPEED
num_machines: 1
num_processes: 1
